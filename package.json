{
  "name": "shudong",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix web install && npm --prefix mobile install",
    "web": "npm --prefix web run dev",
    "mobile": "npm --prefix mobile run start --",
    "typecheck": "npm --prefix web run typecheck && npm --prefix mobile run typecheck"
  }
}
