'use client';
import Link from 'next/link';import {FormEvent,useState} from 'react';import {AuthCard,Notice} from '@/components/AuthCard';import {authError,validEmail} from '@/lib/auth';import {supabase} from '@/lib/supabase';
export default function ForgotPasswordPage(){const [email,setEmail]=useState(''),[message,setMessage]=useState(''),[success,setSuccess]=useState(false),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();const clean=email.trim().toLowerCase();setMessage('');setSuccess(false);if(!validEmail(clean))return setMessage('请输入注册时使用的完整邮箱地址。');setBusy(true);const {error}=await supabase.auth.resetPasswordForEmail(clean,{redirectTo:`${window.location.origin}/reset-password`});setBusy(false);if(error)setMessage(authError(error.message));else{setSuccess(true);setMessage('重置邮件已发送。请检查收件箱和垃圾邮件，并点击其中的链接。')}}return <AuthCard title="找回密码" subtitle="输入注册邮箱，我们会发送一封密码重置邮件。"><form onSubmit={submit} className="space-y-4"><label className="block text-sm font-medium">注册邮箱<input className="field mt-2" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@example.com"/></label><button className="btn w-full" disabled={busy}>{busy?'发送中...':'发送重置邮件'}</button><Notice message={message} success={success}/></form><p className="text-center text-sm"><Link className="text-sage hover:underline" href="/login">返回登录</Link></p></AuthCard>}
